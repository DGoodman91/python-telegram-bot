A telegram bot, built as a platform to build other Python experiments on.

To run:
```shell
cd python-telegram-bot      # needs to be run from this dir
py ./src/run.py             # entry point for the application
```

